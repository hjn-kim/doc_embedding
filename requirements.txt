# 벤치마크 + 대시보드 통합 의존성.
#
#   pip install -r requirements.txt
#
# RunPod PyTorch 템플릿을 쓴다면 torch 는 이미 설치돼 있으니 아래 torch 줄을
# 주석 처리하세요 (덮어쓰면 CUDA 가 깨집니다).
# 단 템플릿의 torch 가 2.5 이상인 것을 고르세요. 2.4.x 에서는 최신 transformers 가
#   ImportError: cannot import name 'DTensor' from 'torch.distributed.tensor'
# 로 죽습니다 (그 공개 경로는 torch 2.5 부터 생겼습니다).
torch>=2.5.0

# ── 임베딩 ────────────────────────────────────────────────────
# transformers 는 4.56 이상이어야 합니다.
#   · 4.51 미만: harrier-oss-v1 이 Qwen3 아키텍처라
#       ValueError: ... model type `qwen3` but Transformers does not recognize
#     로 로드 자체가 실패합니다.
#   · 4.56 미만: harrier 의 config.json 이 정밀도를 신식 "dtype" 키로 적어둬서
#     그 값을 무시하고 fp32 로 올립니다 (동작은 하지만 VRAM 을 두 배 씁니다).
transformers>=4.56.0
sentence-transformers>=4.0.0
accelerate>=1.1.1
huggingface-hub>=0.26.0

# ── BGE-M3 hybrid (선택) ──────────────────────────────────────
# dense+sparse 하이브리드 행을 리포트에 추가합니다. 없으면 src/models.py 가
# ImportError 를 잡아 dense 단독으로 폴백하므로 bge-m3 행 자체는 그대로 나옵니다.
#
# 기본값으로 켜두지 않은 이유: FlagEmbedding 1.3.2 는 transformers==4.44.2 를
# '>=' 가 아니라 '==' 로 못박아서 위의 4.56 요구와 정면 충돌합니다
# (pip 이 ResolutionImpossible 로 설치 자체를 거부). 즉 harrier 와 하이브리드는
# 한 환경에 같이 담을 수 없습니다.
#
# 하이브리드 숫자가 필요하면 별도 가상환경에서 아래 두 줄로 bge-m3 만 돌리고
# summary.csv 를 합치세요:
#     pip install "FlagEmbedding==1.3.2" "transformers==4.44.2" "sentence-transformers==3.3.1"
#     python -m src.run_benchmark --models bge-m3
# FlagEmbedding==1.3.2

# ── 문서 파싱 ─────────────────────────────────────────────────
python-docx>=1.1.0          # .docx
PyMuPDF>=1.24.0             # .pdf (주 파서)
pypdf>=4.2.0                # .pdf (fallback)

# ── 대시보드 (2-4_document_embedding.py) ─────────────────────
# 로컬 실행용. Streamlit Community Cloud 는 저장소 루트의 requirements.txt 를
# 자동 설치하는데, 이 파일에는 torch 까지 들어 있어 리소스 한도를 넘겨
# 배포가 실패합니다. Cloud 에 올릴 거면 대시보드 전용 저장소를 따로 두고
# streamlit + pandas 만 적은 requirements.txt 를 쓰세요.
streamlit>=1.50.0           # st.context.theme, width="stretch" 사용
# altair 는 streamlit 이 함께 설치하므로 명시하지 않는다

# ── 유틸 ──────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
PyYAML>=6.0
tabulate>=0.9.0
